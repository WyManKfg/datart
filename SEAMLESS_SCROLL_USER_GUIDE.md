# 无缝滚动列表插件 (Seamless Scroll List) 使用手册

## 1. 插件简介
**无缝滚动列表 (Seamless Scroll List)** 是为 datart 专门开发的一款可视化插件。它旨在解决在有限的屏幕空间（如数据大屏、仪表板固定组件）内展示大量列表数据的问题。通过自动循环滚动的动画效果，用户可以无需手动操作即可预览所有数据项。

---

## 2. 核心功能
*   **自动化滚动**: 列表内容自动向上平滑滚动，到达底部后无缝衔接至顶部，实现无限循环。
*   **交互控制**: 支持鼠标悬停时自动停止滚动，方便用户精确查看某一行数据，移开后恢复滚动。
*   **高度可定制**: 灵活调整滚动速度、刷新频率、行高及样式，适配不同的展示场景。
*   **多语言支持**: 完整适配 datart 的中英文国际化框架。

---

## 3. 配置指南

### 3.1 数据配置 (Data)
在图表编辑器的“数据”栏目中，需要配置以下维度：
*   **维度 (Dimension)**: 对应列表中显示的左侧标签或名称。
*   **指标 (Metrics)**: 对应列表中显示的右侧数值或结果。

### 3.2 滚动设置 (Scroll Settings)
在“样式” -> “滚动设置”中可配置以下参数：
*   **步长 (Step)**: 每次移动的像素距离。数值越大，滚动速度越快。建议设置为 `1` 或 `2` 以保持平滑感。
*   **时间间隔 (Interval)**: 动画触发的频率（单位：毫秒）。建议设置为 `50ms` 左右。
*   **行高 (Line Height)**: 设置每一行列表的高度（像素）。这会直接影响每屏显示的数据条数。
*   **鼠标悬停停止 (Hover Stop)**: 勾选后，当鼠标光标进入组件区域时，滚动将立即停止。

### 3.3 样式设置 (Style Settings)
*   **字体 (Font)**: 设置列表文本的大小、颜色、粗细及字体族。
*   **背景颜色 (Background Color)**: 设置整个列表容器的背景色，支持透明。

---

## 4. 视觉效果与交互
*   **无缝衔接**: 插件通过在列表末尾克隆数据项，确保从最后一行滚动到第一行时没有视觉跳变或空白。
*   **平滑动画**: 采用 CSS 硬件加速 (Transform) 进行位移，确保在高分辨率大屏上依然能保持 60FPS 的平滑度。
*   **响应式布局**: 组件会自动填满父级容器（Widget），并根据容器宽度自动截断过长的文本（显示省略号）。

---

## 5. 适用场景
1.  **大屏监控**: 实时滚动的告警信息、最新订单通知。
2.  **仪表板**: 空间有限的 Top 10 排名列表、员工名录。
3.  **公示栏**: 需要循环播放的政策条文或通知公告。
